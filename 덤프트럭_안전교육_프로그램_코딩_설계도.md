# 덤프트럭 안전교육 이미지 생성 프로그램 — 코딩 설계도 (v1.0)

> **전제:** 이 문서는 앞서 제공한 「덤프트럭_안전교육_ComfyUI_파이프라인_사양서」와 「덤프트럭_안전교육_법령_리서치_방법론」을 **구현 재료**로 사용한다. 먼저 두 문서를 읽고 개념(캡오버 덤프트럭, 노란 번호판, 한글 후편집, 워크플로우 API)을 잡은 뒤, 이 설계도를 Cursor/Claude Code에 붙여넣기 식으로 진행하면 된다.
>
> **목표물:** ComfyUI를 백엔드로 두고, **YAML 시나리오 설정 → 영문 프롬프트 자동 조립 → 배치 생성 → 통합 저장(manifest) → 검수 로그**까지 처리하는 로컬 Python 프로그램(CLI 우선). 일반 AI 사이트의 1회성 생성 대신 **시드·프롬프트·모델 이력이 남아 재현 가능**하고, LoRA·ControlNet·인페인트를 워크플로우 단위로 재사용하는 구조.
>
> 작성 기준일: 2026-09-04. 노드·모델 버전은 설치 시점에 재확인.

---

## 1. 시스템 아키텍처

```
[Python 앱 — 사용자 PC (코딩으로 구현)]
  main.py ──▶ batch_runner ──▶ prompt_builder ──▶ workflow_loader ──▶ comfy_client
                                                                        │  POST /prompt · WS · GET /view
                                                                        ▼
                                              [ComfyUI 서버 127.0.0.1:8188]
                                              커스텀 노드(ControlNet·IPAdapter·Impact)
                                              모델(체크포인트·LoRA·ControlNet)
                                              workflow.json (API export 형식)
                                                                        │  결과 이미지
                                                                        ▼
                                  [comfy_client 수신] ──▶ manifest.json ──▶ qc 검수 ──▶ outputs/ 저장
```

- **Python 앱**: ComfyUI에 작업을 "지시"하고 결과를 "받아" 관리하는 오케스트레이터
- **ComfyUI 서버**: 실제 생성 엔진(동일 PC에서 `127.0.0.1:8188`로 기동). 직접은 건드리지 않고 HTTP/WebSocket API로만 사용
- **핵심 발상**: 생성 로직은 ComfyUI 워크플로우 JSON에 두고, Python은 그 JSON의 프롬프트·시드·해상도만 치환해 반복 실행 → AI 코딩 에이전트가 만들기 쉽고, ComfyUI GUI에서 시각적으로 수정도 가능

---

## 2. 폴더/파일 트리 (스캐폴드 그대로 생성)

```
dump_truck_safety/                  # 프로젝트 루트 (= 코딩 작업 폴더)
├─ README.md                        # 설치·실행 순서 (산출물 우선순위 0)
├─ requirements.txt                 # requests, websocket-client, PyYAML
├─ config/
│  ├─ settings.yaml                 # 서버 주소·기본 해상도·시드·공통 프롬프트·네거티브
│  └─ scenes/
│     ├─ A_blindspot.yaml           # 시나리오 4종 (사양서 §5 기준)
│     ├─ B_reverse_guide.yaml
│     ├─ C_unload.yaml
│     └─ D_precheck.yaml
├─ src/
│  ├─ main.py                       # CLI 엔트리 (argparse)
│  ├─ prompt_builder.py             # 공통+장면 블록 조립, 네거티브 고정
│  ├─ workflow_loader.py            # workflow_api.json 로드·치환
│  ├─ comfy_client.py               # HTTP/WebSocket 통신
│  ├─ batch_runner.py               # 시나리오 루프·와일드카드·재시도
│  ├─ manifest.py                   # manifest.json 기록
│  └─ qc.py                         # 검수 체크리스트 로그
├─ workflows/                       # ComfyUI에서 "Save (API Format)"로 export한 JSON
│  ├─ t2i_base.json                 # 1차 텍스트→이미지
│  ├─ i2i_refine.json               # 2차 레퍼런스 보정
│  └─ controlnet_chain.json         # 3차 ControlNet 구도 고정
├─ assets/refs/                     # 레퍼런스 실사 (i2i·IPAdapter 입력)
└─ outputs/
   ├─ scene_A/ ...                  # 검수 통과 컷
   ├─ post/                         # 한글 후편집 완료본 (교육자료 최종 사용)
   └─ logs/manifest.json            # 생성 이력
```

※ **ComfyUI 자체는 별도 폴더**(예: `~/ComfyUI`)에 설치. 이 프로그램은 모델 경로만 알고 API로 접근한다.

---

## 3. 모듈별 입출력 계약

> 코딩에서 "파일·함수 시그니처·스키마"를 이대로 주면, 에이전트가 흔들리지 않고 구현한다.

### 3.1 `prompt_builder.py`

```
def build_positive(scene: SceneConfig, wildcards: dict[str, str] | None = None) -> str
def build_negative() -> str                       # settings.yaml의 negative_prompt 반환
def build_prompt(scene: SceneConfig, wildcards: dict[str, str] | None = None) -> PromptResult
# PromptResult = {"positive": str, "negative": str}
```

- **조립 규칙 (사양서 §5 재사용):** `positive = common_prompt + ", " + scene.scene_block` + (와일드카드 치환). `negative = settings.yaml 고정값 + scene.negative_extra`
- common_prompt(settings.yaml 기본값, 사양서 §5 공통 블록):

```yaml
common_prompt: >-
  Korean cab-over heavy-duty dump truck, Hyundai Xcient-style 25.5-ton,
  flat front cab, left-hand drive, yellow commercial license plate,
  dump body loaded with soil, driver wearing white safety helmet and
  yellow reflective safety vest, Korean construction site, apartment
  complex under construction, Hangul signage, orange safety barriers,
  right-hand traffic road with white lane markings,
  photorealistic safety-training manual photography, 4K, bright daylight
negative_prompt: >-
  conventional-nose American truck, right-hand drive, European blue license plate,
  English signage, foreign lettering, left-hand traffic, foreign cityscape,
  worker without safety helmet, baseball cap, blurred text, deformed dump body,
  extra wheels, mirror error, extra fingers
```

### 3.2 SceneConfig — 시나리오 YAML 스키마

```yaml
# config/scenes/A_blindspot.yaml
scene_id: A_blindspot
scene_block: >-
  dump truck making a RIGHT turn at a downtown intersection,
  RIGHT-side blind spot zone outlined with red-yellow warning overlay,
  worker in safety vest waiting at curb
negative_extra: []
seed: 1001            # 고정 → 재현 보장. 0이면 랜덤
count: 4              # 배치 수 (와일드카드 사용 시 무시)
width: 1024
height: 1024
steps: 30
cfg: 6.0
workflow: t2i_base    # workflows/ 아래 키
```

- 나머지 3종은 `scene_block`만 교체 (사양서 §5 표 그대로):
  - `B_reverse_guide`: `dump truck reversing slowly at an earth-loading area, signal man in yellow reflective vest holding orange signal wand standing at rear corner directing driver`
  - `C_unload`: `dump truck raising dump body to unload soil at embankment, guided by ground worker, orange safety barriers around work radius`
  - `D_precheck`: `driver doing pre-departure walk-around inspection, checking tires and cargo cover, truck parked at construction site entrance`

### 3.3 `workflow_loader.py`

```
def load(path: str) -> dict
def set_text(workflow: dict, node_title: str, text: str) -> dict   # CLIPTextEncode 치환
def set_seed(workflow: dict, seed: int) -> dict                    # KSampler(noise_seed)
def set_size(workflow: dict, w: int, h: int) -> dict               # EmptyLatentImage
```

- **치환 식별 규칙(중요):** ComfyUI의 API export JSON에서 각 노드는 `class_type`과 `_meta.title`을 가진다. 본 프로그램은 **`_meta.title` 문자열로 노드를 찾는다**. 따라서 ComfyUI GUI에서 노드 title을 아래 규약으로 고정해야 한다:

| 노드 | title 고정값 |
|---|---|
| 긍정 프롬프트 CLIPTextEncode | `positive` |
| 부정 프롬프트 CLIPTextEncode | `negative` |
| KSampler | `KSampler` |
| EmptyLatentImage | `EmptyLatentImage` |

- 항상 `json.loads(json.dumps(workflow))`로 **deep copy** 후 치환(원본 오염 방지)

### 3.4 `comfy_client.py`

```
class ComfyClient:
    def __init__(self, base_url: str = "http://127.0.0.1:8188")
    def queue_prompt(self, workflow: dict) -> str                    # POST /prompt → prompt_id
    def wait_done(self, prompt_id: str, timeout: float = 600) -> None  # WS /ws 대기
    def fetch_images(self, prompt_id: str, out_dir: Path) -> list[Path]  # GET /history → GET /view
```

- **통신 순서 (사양서 §9 재사용):** `POST /prompt`(workflow + client_id) → `WS ws://127.0.0.1:8188/ws?clientId=...`로 `executing` 메시지가 `node: null`이 될 때까지 대기 → `GET /history/{prompt_id}`에서 출력 이미지 파일명 획득 → `GET /view?filename=...&subfolder=...&type=...`로 바이트 저장
- client_id는 `uuid.uuid4()`로 매번 생성, WS 쿼리 파라미터로 전달
- 실패 시: HTTP 4xx/5xx는 응답 본문 로그, WS 타임아웃은 timeout 파라미터 상향

### 3.5 `batch_runner.py`

```
def run_scene(client: ComfyClient, scene: SceneConfig,
              wf_pool: dict[str, dict], out_dir: Path) -> BatchResult
# for i in range(scene.count):
#     seed = scene.seed + i   (고정 시드면 일관성, 아니면 random)
#     workflow = deep copy → set_text/set_seed/set_size → queue → wait → fetch
#     파일명: {scene_id}_{seed}_{unixtime}.png
# 개별 실패: 2회 재시도, 그래도 실패 시 manifest에 status=failed 기록 후 계속
```

- 와일드카드(예: `__weather__ ↔ bright daylight / overcast / dusk`)는 조합 수만큼 count 대신 루프. 조합 폭주 방지를 위해 **조합 수 상한(기본 12)** 을 두고 초과 시 경고

### 3.6 `manifest.py` — manifest.json 스키마

```json
{
  "version": 1,
  "generated_at": "2026-09-04T12:00:00+09:00",
  "entries": [
    {
      "id": "A_blindspot_1001_1720000000.png",
      "scene_id": "A_blindspot",
      "seed": 1001,
      "workflow": "t2i_base",
      "checkpoint": "realvisxl_v5.safetensors",
      "lora": [],
      "positive": "...",
      "negative": "...",
      "status": "ok"
    }
  ]
}
```

- 파일명 규칙: `{scene_id}_{seed}_{unixtime}.png` → 중복 없음 + 시드 재현 가능
- 기록 값은 전부 settings.yaml·scene YAML·workflow에서 **실제 사용한 값**을 적는다 (에이전트가 "추측 입력"하지 않도록 명시)

### 3.7 `qc.py`

- 사양서 §10의 9항목 체크리스트를 `config/qc_rules.yaml`로 로드, 이미지별 `{pass/fail}` 로그(`outputs/logs/qc_{scene_id}.json`)
- fail 항목별 후속 액션 태그 예: 표지판 괴문자 → `post/` 이동 + 후편집 필요, 얼굴·손가락 이상 → `FaceDetailer` 재실행 제안

### 3.8 `main.py` (CLI)

```
python src/main.py generate --scene A_blindspot --count 4
python src/main.py generate --all
python src/main.py qc --scene A_blindspot
python src/main.py manifest --show A_blindspot
```

---

## 4. ComfyUI workflow.json 연결 규칙

1. **export 방법:** ComfyUI GUI에서 파이프라인 완성 → 우상단 메뉴 → **Save (API Format)** → `workflows/*.json`
2. **노드 title 고정 규약:** §3.3 표 그대로(`positive`/`negative`/`KSampler`/`EmptyLatentImage`). title이 다르면 Python 치환이 조용히 무시되므로 에이전트에게 "치환 전 `_meta.title` 프린트로 검증"을 요구한다
3. **커스텀 노드 필수 3종 (사양서 §3, 전부 공식 저장소 확인됨):**
   - `ComfyUI-Advanced-ControlNet` → `ControlNetLoaderAdvanced`, `Apply ControlNet (Advanced)` (github.com/Kosinkadik/ComfyUI-Advanced-ControlNet)
   - `ComfyUI-Impact-Pack` → `FaceDetailer`, `DetailerForEach (SEGS)` (github.com/ltdrdata/ComfyUI-Impact-Pack)
   - `ComfyUI_IPAdapter_plus` → `IPAdapterUnifiedLoader`, `IPAdapter` (github.com/cubiq/ComfyUI_IPAdapter_plus)
   - 설치: ComfyUI Manager → Custom Nodes Manager → 검색·설치 → 재시작
4. **워크플로우별 골격:**
   - `t2i_base.json`: CheckpointLoaderSimple → CLIPTextEncode(positive/negative) → KSampler → VAEDecode → SaveImage
   - `i2i_refine.json`: LoadImage → VAEEncode → (프롬프트 치환 동일) → KSampler(denoise 0.30~0.45, 사양서 §6)
   - `controlnet_chain.json`: t2i_base + ControlNetLoaderAdvanced 3개(Depth/Canny/OpenPose) 직렬 체인, 강도 depth 0.5~0.7·canny 0.4~0.6·openpose 0.6~0.8, 합계 1.2 이하 (사양서 §7)
   - 인페인트판(표지판·번호판 마스크): `SetLatentNoiseMask` + `VAEEncodeForInpaint` (ComfyUI 내장 노드), denoise 0.8~1.0

---

## 5. 로컬 실행 환경 (GPU/모델 배치)

| 항목 | 값 |
|---|---|
| Python | 3.10+ |
| 패키지 | requests · websocket-client · PyYAML |
| ComfyUI 기동 | `python main.py --listen 127.0.0.1:8188` |
| 체크포인트 | `~/ComfyUI/models/checkpoints/` (RealVisXL 류, Civitai에서 사진 리얼+다운로드 상위 기준 선정) |
| LoRA | `~/ComfyUI/models/loras/` (국산 덤프 전용은 미확정 → 사양서 §4의 3택1 전략) |
| ControlNet 모델 | `~/ComfyUI/models/controlnet/` |
| IPAdapter·CLIP Vision | `~/ComfyUI/models/ipadapter/`, `~/ComfyUI/models/clip_vision/` |
| VRAM 부족 대체 | 12GB 미만: `--medvram`, 해상도 768↓, SD1.5 체크포인트로 축소. Flux 계열은 24GB 권장 |

**settings.yaml 예시:**
```yaml
comfy_base_url: http://127.0.0.1:8188
default_width: 1024
default_height: 1024
default_seed: 1001
default_steps: 30
default_cfg: 6.0
checkpoint: realvisxl_v5.safetensors
workflow_dir: workflows
output_dir: outputs
common_prompt: |-
  (3.1의 common_prompt 내용)
negative_prompt: |-
  (3.1의 negative_prompt 내용)
```

---

## 6. 데이터 흐름 예시 (1회 배치)

1. `python src/main.py generate --scene A_blindspot --count 4`
2. batch_runner → `config/scenes/A_blindspot.yaml` 로드
3. prompt_builder → `positive = common_prompt + scene_block`, `negative = 고정값`
4. workflow_loader → `workflows/t2i_base.json` deep copy → positive/negative/seed(1001+i)/size 치환
5. comfy_client → `POST /prompt` → WS 대기 → `GET /history` → `GET /view` 4장 다운로드
6. manifest.py → entries 4건 추가
7. 콘솔: `outputs/scene_A/A_blindspot_1001_....png` 생성 완료 + `outputs/logs/manifest.json` 갱신

---

## 7. 개발 단계별 코딩 프롬프트 & 완료 판정 기준

> 각 프롬프트를 그대로 Cursor/Claude Code에 붙여넣고, "완료 판정" 항목이 통과하면 다음 단계로. 한 단계가 끝날 때마다 에이전트에게 **판정 항목을 실행하라고 요구**할 것.

| 단계 | 산출물 | 완료 판정 기준 (직접 재현 가능한 테스트) |
|---|---|---|
| A | 프로젝트 스캐폴드 + README | `python -c "import requests, websocket, yaml"` 통과, §2 폴더 트리 일치, `python src/main.py --help` 출력 |
| B | ComfyUI 환경 셋업 | ComfyUI가 8188 포트로 기동, 커스텀 노드 3종이 GUI에 표시 |
| C | workflow_loader·comfy_client | mock JSON 치환 유닛테스트 통과 + t2i_base로 실제 1장 생성(테스트 프롬프트) |
| D | batch_runner·manifest·CLI | `generate --scene A_blindspot --count 2` → PNG 2장 + 유효한 manifest.json |
| E | i2i·ControlNet·인페인트 | 레퍼런스 차량 실루엣 유지(denoise 0.4) 눈 검수, 3체인 강도 적용 확인 |
| F | qc·(선택)웹 UI | `qc --scene A_blindspot` → 체크리스트 JSON 생성 |

### 단계별 프롬프트 (붙여넣기용)

**A. 스캐폴드**
> Python 3.10 프로젝트를 `dump_truck_safety/`에 만들어줘.
> 1) 폴더: `config/scenes`, `src`, `workflows`, `assets/refs`, `outputs/scene_A`, `outputs/post`, `outputs/logs`
> 2) `requirements.txt`: requests, websocket-client, PyYAML
> 3) `config/settings.yaml`: §5 예시값(comfy_base_url, common_prompt, negative_prompt 등) 그대로
> 4) `src/`에 모듈 7개 생성. 함수 시그니처는 §3 계약 그대로, 본문은 `pass` 또는 최소 stub, docstring에 계약 설명
> 5) `config/scenes/`에 4개 YAML(§3.2 스키마, scene_block은 사양서 §5 표의 영문 장면 블록)
> 6) `README.md`에 설치(`pip install -r requirements.txt`)·ComfyUI 기동·실행 명령 정리
> 완료 후: `python -c "import requests, websocket, yaml"`와 `python src/main.py --help`가 통과하도록 하고, 실패하면 고쳐줘.

**B. ComfyUI 환경 셋업**
> README에 아래 내용을 추가 정리해줘.
> 1) ComfyUI 설치(공식 git clone 또는 포터블), `python main.py --listen 127.0.0.1:8188` 기동법
> 2) ComfyUI Manager 설치 후 커스텀 노드 3종 설치: ComfyUI-Advanced-ControlNet, ComfyUI-Impact-Pack, ComfyUI_IPAdapter_plus (설치 후 재시작)
> 3) 모델 배치: RealVisXL 류 체크포인트 → `models/checkpoints/`, ControlNet 모델(depth/canny/openpose) → `models/controlnet/`
> 4) VRAM별 대체안: `--medvram`, SD1.5 축소
> 완료 후: ComfyUI GUI에서 노드 3종이 보이는지 확인 절차를 README에 적어줘.

**C. 워크플로우 로드/치환 + API 클라이언트**
> `src/workflow_loader.py`와 `src/comfy_client.py`를 §3.3·§3.4 계약대로 구현해줘.
> - workflow_loader: `_meta.title`로 노드를 찾아 text/seed/size 치환. `json.loads(json.dumps(...))` deep copy 후 수정. 치환 결과에서 변경된 노드를 반환
> - comfy_client: `POST /prompt`(client_id 포함) → `ws://{host}/ws?clientId=` 대기 → `GET /history/{id}` → `GET /view` 다운로드
> - `tests/test_workflow_loader.py`: 실제 `workflows/t2i_base.json`을 export해서 사용 — positive/negative/seed 치환이 정확히 반영되는지 assert
> 완료 후: 테스트가 통과하도록 하고, 실패 시 수정해줘.

**D. 배치 러너·매니페스트·CLI**
> `src/batch_runner.py`, `src/manifest.py`, `src/main.py`를 §3.5~§3.8 계약대로 구현해줘.
> - 파일명 `{scene_id}_{seed}_{unixtime}.png`, manifest 스키마 §3.6 준수, 실패 시 재시도 2회 후 status=failed 기록
> - main.py: generate(--scene/--count/--all), qc(--scene), manifest(--show) 서브커맨드
> 완료 후: ComfyUI가 켜진 상태에서 `python src/main.py generate --scene A_blindspot --count 2` 실행 → PNG 2장과 유효한 JSON을 보여주고, 안 되면 원인을 찾아 고쳐줘.

**E. i2i·ControlNet·인페인트 워크플로우**
> `workflows/i2i_refine.json`, `workflows/controlnet_chain.json`을 만들고 배치에서 선택 가능하게 해줘.
> - i2i: LoadImage → VAEEncode 경로, denoise 0.30~0.45 (사양서 §6)
> - controlnet: ControlNetLoaderAdvanced 3개 직렬, 강도 depth 0.5~0.7 / canny 0.4~0.6 / openpose 0.6~0.8, 합 1.2 이하 (사양서 §7)
> - 노드 title 규약(§3.3)을 반드시 지킬 것
> 완료 후: `assets/refs/`의 국내 덤프 실사 1장으로 i2i 1컷 생성 → 차량 실루엣이 유지되는지 확인 절차를 README에 적어줘.

**F. QC·(선택)웹 UI**
> `src/qc.py`를 §3.7 계약대로 구현. `config/qc_rules.yaml`에 사양서 §10의 9항목을 기본값으로 추가.
> 완료 후: `python src/main.py qc --scene A_blindspot`이 체크리스트 JSON을 생성하는지 확인.
> (선택) 같은 인터페이스로 이미지 미리보기·pass/fail 클릭이 되는 간단한 로컬 웹 UI(FastAPI/Flask)를 추가해줘.

---

## 8. 트러블슈팅 (초보자 클리닉)

| 증상 | 원인·대응 |
|---|---|
| 커스텀 노드가 GUI에 안 보임 | Manager → Custom Nodes Manager에서 3종 재설치 후 **ComfyUI 완전 재시작**. `custom_nodes/` 폴더에 git clone 결과가 있는지 확인 |
| API 연결 거부(Connection refused) | ComfyUI가 `--listen 127.0.0.1:8188`로 켜져 있는지, settings.yaml의 `comfy_base_url` 일치 여부 |
| WS 응답이 계속 안 옴 | client_id 불일치가 99% 원인. `queue_prompt`에 넣은 client_id를 WS 쿼리에 동일하게 사용. timeout 기본 600s |
| 치환한 프롬프트가 반영 안 됨 | 노드 `_meta.title`이 `positive`/`negative`/`KSampler`/`EmptyLatentImage`와 정확히 일치하는지. export된 JSON에서 직접 키 확인 |
| VRAM 부족(OOM) | `--lowvram` 또는 `--medvram`, 해상도 768↓, SD1.5 체크포인트로 교체, fp8 양자화 모델 |
| 결과가 외국풍(미국 보닛 트럭) | LoRA/레퍼런스 img2img 병행 + 네거티브 확인 + 시드 고정. 특히 **국산 덤프 LoRA는 Civitai에서 미확정**이므로 자체 LoRA 학습 또는 레퍼런스 i2i로 대체(사양서 §4) |
| 한글 표지판이 괴문자 | 정석은 **후편집**(사양서 §12): 생성 시 한글 요구 + 검수에서 fail 태그 → `outputs/post/`로 이동해 동일 폰트로 덮어쓰기. 마스크 인페인트(denoise 0.8~1.0)로 일부 완화 |
| 모델 다운로드 경로 오류 | 체크포인트는 `models/checkpoints/`, LoRA는 `models/loras/`, ControlNet은 `models/controlnet/`, IPAdapter는 `models/ipadapter/` + `models/clip_vision/`. 파일명을 settings.yaml/워크플로우에 **정확히** 입력 |
| manifest.json 손상 | utf-8 고정, 스키마 §3.6 준수, 쓰기 시 임시 파일 → rename 방식 권장 |
| 이미지가 뜬금없이 다르게 나옴 | 같은 시드+같은 프롬프트+같은 모델이어야 재현. LoRA/워크플로우 버전도 이력(manifest)에 기록 |

---

## 9. 구현 순서 요약 & 학습 우선순위

1. **개념 학습(먼저):** 사양서 §5(프롬프트 블록) → §9(API 통신) → 법령 리서치 §3(법령 조회법)
2. **구현 순서:** A 스캐폴드 → B 환경 셋업 → C 로더·클라이언트 → D 배치 → E 고급 워크플로우 → F QC
3. **마일스톤:** "ComfyUI GUI로 1장" → "CLI로 4장" → "레퍼런스 유지" → "전 시나리오 배치"

## 10. 참고 자료

- ComfyUI API: https://docs.comfy.org/development/comfyui-server/api-examples
- ControlNet 가이드: https://docs.comfy.org/tutorials/controlnet/controlnet
- Advanced ControlNet: https://github.com/Kosinkadink/ComfyUI-Advanced-ControlNet
- Impact Pack: https://github.com/ltdrdata/ComfyUI-Impact-Pack
- IPAdapter Plus: https://github.com/cubiq/ComfyUI_IPAdapter_plus

*작성 기준일: 2026-09-04. 법령·노드·모델 버전은 사용 시점에 재확인.*
