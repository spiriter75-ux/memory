# 덤프트럭 건설현장 안전교육 — 동적 모듈형 9대 슬롯 및 MiniMax H3 I2VA 매핑 사양서 (v2.0)

> **핵심 원칙:** "현장은 광범위하다." 사토장, 골재장, 도심 교차로, 고속도로, 지하 터널, 철거지 등 작업 환경과 계절이 천차만별이므로 **DNA를 단일 문자열로 고정(Hardcoding)하지 않고, [절대 불변 원칙] 위에 [현장별 가변 프리셋 라이브러리]를 동적으로 조립**하는 모듈형 레지스트리 구조를 강제한다.
>
> **검수 기준일:** 2026-09-04  
> **적용 엔진:** ComfyUI SDXL 2D 고증 ➔ MiniMax H3 Ref2VA (`Minimax H3 Ref2VA + Easy Cache _ Spectrum + Sage`)

---

## 1. 절대 불변 원칙 (Absolute Invariants — 모든 현장 공통 강제)

어떤 현장, 어떤 날씨, 어떤 작업이라도 아래 6대 원칙은 100% 고정되어야 하며 절대 타협하지 않는다:

1. **차량 구조:** 대한민국 25.5톤 캡오버(평두형, 보닛 없음), 좌핸들(LHD), 주황색 건설기계 번호판, 4축 8륜(8x4).
2. **도로 규칙:** 대한민국 우측통행(Right-Hand Traffic), 황색 복선 중앙선.
3. **인물 PPE:** 현장 하차 시 안전모(Safety Helmet) + 형광 반사조끼(Hi-Vis Vest) + 안전화(Safety Boots) 100% 착용.
4. **물리 메커니즘:** 100% Full Air Brake (7bar 공기압 충전, 브레이크 페이드 방지). 승용차용 베이퍼록 용어 사용 금지.
5. **법규 준수:** 2026 첨단 안전장치(AEBS, LDWS, 어라운드뷰 모니터), 현장 제한속도 10~20km/h.
6. **Zero-Bubble:** AI 이미지 내부 만화 말풍선 및 문자 생성 100% 배제 (`no speech bubble, no text, no watermark`).

---

## 2. 동적 가변 매트릭스 (Dynamic Field Presets Registry)

광범위한 건설현장을 완벽히 포괄하기 위해 4대 축의 동적 프리셋을 라이브러리화하여 시나리오별로 자유롭게 조립한다.

### 2.1 [축 1] 현장 작업 구역 프리셋 (`site_presets`)
| 프리셋 키 | 현장 작업 구역 | 핵심 시각 요소 및 고증 배경 |
|---|---|---|
| `embankment_dump` | 사토장 / 성토지 / 간척지 | 연약지반 흙사면, 지반 침하 위험 흙길, 덤핑 하역 구역, 배수로 |
| `aggregate_quarry` | 석산 / 골재 채취장 / 쇄석장 | 거친 암석/자갈 바닥, 대형 굴착기/로더 상차 구역, 비산먼지 방진막 |
| `urban_crossroad` | 도심 도로 / 교차로 / 스쿨존 | 횡단보도 정지선, 보행자 신호등, 도심 빌딩, 우회전 사각지대 구역 |
| `highway_haul` | 고속도로 / 자동차 전용도로 | 아스팔트 고속 주행로, 방음벽, 가드레일, 안전거리 100m 안내판 |
| `tunnel_underground` | 지하 터널 / 지하철 굴착 현장 | 암전 저조도, 가설 나트륨 인공조명, 환기 덕트, 비좁은 암반 굴착로 |
| `demolition_site` | 도심 재건축 / 건물 철거 현장 | 깨진 콘크리트 잔해, 폐철근 펑크 위험 노면, RPP 방음 펜스 |

### 2.2 [축 2] 차량 작업 및 적재 상태 프리셋 (`cargo_states`)
| 프리셋 키 | 적재 및 차량 상태 | 프롬프트 주입 키워드 |
|---|---|---|
| `loaded_soil` | 토사 만차 상태 | `dump body fully loaded with heavy brown construction soil, heap piled up` |
| `loaded_rocks` | 거친 암석/골재 만차 | `dump body loaded with jagged quarry stones and sharp gravel rocks` |
| `empty_bed` | 하역 후 공차 상태 | `empty steel dump body lowered flat, clean inside bed` |
| `dumping_lift` | 적재함 유압 상승 하역 | `dump body raised high to steep 45-degree angle by hydraulic cylinder, unloading soil` |
| `cab_inspection` | 정차 및 일일 점검 상태 | `parked safely with yellow wedge chocks behind rear tires, cabin tilted or doors closed` |

### 2.3 [축 3] 기상 및 노면 환경 프리셋 (`weather_presets`)
| 프리셋 키 | 기상 및 노면 조건 | 위험 메커니즘 시각화 |
|---|---|---|
| `dry_daylight` | 맑은 한낮 / 건조 | `bright sunny daylight, dry construction dust cloud around wheels` |
| `heavy_rain_mud` | 폭우 / 장마철 진흙탕 | `heavy rain pouring, wet muddy slurry road, tire sinking into mud, windshield wipers` |
| `winter_black_ice` | 동절기 혹한 / 결빙 | `freezing winter, snowy icy road surface, black ice hazard, snow chains on heavy tires` |
| `night_fog_dim` | 야간 / 짙은 안개 | `dark night with dense fog, bright LED headlights beam cutting through fog, flashing hazard lights` |

### 2.4 [축 4] 인력 및 PPE 역할 프리셋 (`role_presets`)
| 프리셋 키 | 등장인물 및 역할 | 외형 및 행동 지시문 |
|---|---|---|
| `driver_cabin` | 기사님 운전석 탑승 | `driver sitting in left driver seat, wearing seatbelt, glancing at around-view monitor` |
| `driver_precheck` | 기사님 하차 점검 | `driver wearing yellow helmet and reflective vest, inspecting heavy truck tire tread and air pressure` |
| `spotter_guide` | 후방 전담 신호수(유도원) | `signal spotter in hi-vis orange vest and white helmet, holding illuminated red baton directing reversing truck` |
| `supervisor_inspect` | 현장소장 (정반장) | `authoritative supervisor in white helmet and safety vest, holding clipboard and safety inspection checklist` |

---

## 3. 동적 조립 메커니즘 (`src/prompt_builder.py`)

프롬프트 빌더는 고정된 텍스트를 복사하는 것이 아니라, 시나리오 YAML에 정의된 프리셋 키를 조합하여 최종 프롬프트를 자동 생성한다:

```python
# 조합 공식
final_positive = (
    INVARIANT_CORE_TRUCK       # 1. 절대 불변 캡오버 트럭 규격
    + ", " + site_preset       # 2. 광범위 현장 구역 (사토장/석산/도심/터널 등)
    + ", " + cargo_state       # 3. 적재 상태 (만차/공차/덤핑상승)
    + ", " + weather_preset    # 4. 기상/노면 조건 (진흙탕/빙판/맑음/안개)
    + ", " + role_preset       # 5. 인물 및 보호구 (운전자/신호수/안전관리자)
    + ", " + scene_action      # 6. 이번 시나리오의 핵심 위험/예방 동작
    + ", " + STYLE_ZERO_BUBBLE # 7. 실사 4K Zero-Bubble 스타일 앵커
)
```

---

## 4. MiniMax H3 Ref2VA 다중 이미지 연결

`Minimax H3 Ref2VA + Easy Cache _ Spectrum + Sage.json` 워크플로우에 현장 프리셋을 동적 매핑한다:

* **Node 13 (`LoadImage` — Picture 1)**:  
  위 동적 조립기로 생성되어 9대 QC를 통과한 **`winner.png` (0.00초 실제 시작 프레임)**
* **Node 14 (`LoadImage` — Picture 2)**:  
  시나리오에 따라 동적으로 선택되는 2차 보조 참조 이미지:
  - 신호수 시나리오 ➔ `assets/refs/spotter_ref.png`
  - 출발 전 점검 시나리오 ➔ `assets/refs/tire_chock_ref.png`
  - 덤핑 전도 시나리오 ➔ `assets/refs/hydraulic_cylinder_ref.png`
* **Node 15 (`H3 모션 프롬프트`)**:
  ```text
  For the target video, at 0.00 seconds into the target video, <Picture 1> (from [Shot 1]) is fully referenced.
  [Shot 1] The heavy Korean cabover dump truck from <Picture 1> moves at 10km/h on the {site_type} road.
  {motion_action_description}
  overall_soundscape: heavy diesel truck engine idling, reverse warning beeps, construction site ambience.
  ```

---

## 5. I2V 릴레이 연속 모션 (완전 가변 길이 릴레이)

**※ 길이 고정 금지 원칙:** H3는 컷당 **3초 ~ 15초 가변 렌더링**을 네이티브로 지원하며, 시나리오의 작업량과 목적에 따라 총 길이는 **단일 3초 숏폼부터 N개 컷을 연결한 30초, 60초, 100초 이상까지 완전 가변**으로 작동한다.

1. **컷별 개별 가변 렌더링 (3~15초 자유 지정)**:
   - 빠른 핵심 경고/출발 전: 3~5초
   - 복합 주행/사각지대 접근: 5~10초
   - 토사 덤핑 유압 하역/신호수 전면 유도: 8~15초
   - 시나리오 YAML의 `duration` 값에 따라 H3 Node 27(`PrimitiveFloat`)을 동적 제어
2. **N개 컷 프레임 릴레이 및 FFmpeg 병합**:
   - 1개 컷 단독 영상(3~15초)부터 3단 컷툰 릴레이(원인 ➔ 위기 ➔ 해결), 다중 공정 릴레이(N개 컷)까지 원하는 컷 수만큼 생성
   - FFmpeg `concat` 및 `xfade`(크로스페이드) 필터를 적용하여 끊김 없는 가변 길이(3초~100초+) 완결형 교육 비디오로 자동 합성.
